# Flask Email Campaign (Starter)

### Setup

1. Create `.env` (sample provided)
2. Run:

```bash
pip install -r requirements.txt
flask db init
flask db migrate -m "Initial"
flask db upgrade
python run.py
```
